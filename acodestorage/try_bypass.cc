#include <iostream>
using namespace std;

int main() {

	/*
	while (true)

		// whichever
		string input;
		int input;


		//for single inputs
		cin >> input; 
		cin.clear();
		cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');

		cin.clear();
		getline(cin, input); for full input

		// Remember input checking
		if (!cin) return 0;
		
		if (input == 1) cout << "Hello\n";
		if (input == 2) cout << "World\n";
		if (input == "string") cout << "Test\n";
		if (input = "q") return 0;


	*/
	
}
